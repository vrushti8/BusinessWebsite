function Navbar() {
  return (
    <nav className="navbar">
      <div className="nav-logo">Spice Haven</div>

      <div className="nav-links">
        <a href="#menu">Menu</a>
        <a href="#reserve">Reservation</a>
        <a href="#contact">Contact</a>
      </div>
    </nav>
  );
}

export default Navbar;
